@model IEnumerable<CarTrade.Models.Auto>

@{
    ViewBag.Title = "Index";
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
   

@foreach (var item in Model) {
    <tr>
        <td>
            <h5>@Html.DisplayFor(modelItem => item.marka)</h5>
        </td>
        <td>
            <h5>@Html.DisplayFor(modelItem => item.tipus)</h5>      
        </td>
        <td>
            <h5>@Html.DisplayFor(modelItem => item.evjarat)</h5>
        </td>
        </tr>
    <tr>
        <td></td>
        <td>
            @Html.DisplayFor(modelItem => item.gyartasiIdo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.allapot)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tulajdonosokSzama)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.telephelyId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
