@model IEnumerable<CarTrade.Models.Telephely>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.irSzam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cim)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.parkolohely)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.irSzam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cim)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.parkolohely)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
